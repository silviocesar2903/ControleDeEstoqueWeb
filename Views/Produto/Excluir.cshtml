@model ProdutoModel
@{
    ViewBag.Subtitulo = "Exclusão de Produtos";
}

<h1 class="text-primary">@ViewBag.Subtitulo</h1>
<hr>
<h4>Deseja realmte excluir o produto <span class="text-danger">@Model.Nome?</span></h4>


<form asp-action="Excluir">
    <input type="hidden" asp-for="@Model.IdProduto">
    <div class="mt-3">
        <a asp-action="Index" class="btn btn-success">Não</a>
        <button class="btn btn-danger" type="submit">Sim</button>
    </div>
</form>